# Example audit policy snippet for kube-apiserver to capture RBAC-relevant events
apiVersion: audit.k8s.io/v1
kind: Policy
rules:
  - level: RequestResponse
    verbs: ["create","update","patch","delete","bind","impersonate"]
    resources:
      - group: "rbac.authorization.k8s.io"
